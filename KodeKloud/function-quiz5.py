def print_info(name, age=18): # Corrected the syntax by adding a comma between parameters
    print(name, age) # Added closing parenthesis

print_info("john'") # Corrected the string by adding a closing quote
