def my_function(numbers): # Define a function that takes a list of numbers as input
    for i in numbers: # Iterate through each number in the list
        print(i+1, end=' ') # Increment each number by 1 and print it with a space separator

numbers = [1, 2, 3] # Define a list of numbers
my_function(numbers) # Call the function with the list of numbers